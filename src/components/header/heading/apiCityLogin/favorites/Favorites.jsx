import { NavLink } from "react-router-dom";

const Favorites = () => {
    return ( 
        <div>
            <NavLink to='/favorites'>Favorites</NavLink>
        </div>
     );
};
 
export default Favorites;